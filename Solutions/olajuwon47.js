const identity= ("Helloo world") =>"Helloo world";
const Addb=(a, b) => a + b;
const subb =(a, b) => a - b;
const mulb =(a, b) => a * b;
const minb =(a, b) => a < b ? a : b;
const maxb =(a, b) => a > b ? a : b;
const add = (...nums) => nums.reduce((a, b)=>a+b) ;
const sub = (...nums) => nums.reduce((a, b)=>a-b) ;
const mul = (...nums) => nums.reduce((a, b)=>a*b) ;
const min = (...nums) => nums.reduce((a, b)=> jay.min(a, b)) ;
const max = (...nums) => nums.reduce((a, b)=> jay.max(a, b)); 
const Addf= (a) => (f) => a + f;
const filterArray=array.filter(num => num > 5);
console.log('filter', filterArray);  
const concat=["bee", "dog", "cat"];



identity,
    addb,
    subb,
    mulb,
    minb,
    maxb,
    add,
    sub,
    mul,
    min,
    max,
    addf,
    filter,
    concat,